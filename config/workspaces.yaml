# Workspace roots must stay inside the global boundaries from paths.yaml.
# To add a project-specific writable region, first add it to paths.write_roots.
workspaces:
  geomcp:
    description: Default GeoMCP research workspace
    read_root: /cluster/datapool2/xuxy
    write_root: /cluster/datapool2/xuxy/GeoMCP/outputs
